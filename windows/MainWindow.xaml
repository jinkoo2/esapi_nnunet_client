<Window x:Class="nnunet_client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ESAPI nnUNet Client" Height="400" Width="500"
        FontFamily="Arial" FontSize="12"
        WindowStartupLocation="CenterScreen">
    
    <Window.Resources>
        <Style TargetType="{x:Type Button}">
            <Setter Property="Padding" Value="15,10" />
            <Setter Property="Margin" Value="10" />
            <Setter Property="Background" Value="#007ACC" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="HorizontalAlignment" Value="Stretch" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#005a99" />
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#004d80" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <TextBlock Grid.Row="0" 
                   Text="ESAPI nnUNet Client" 
                   FontSize="24" 
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,20"/>
        
        <!-- Buttons -->
        <StackPanel Grid.Row="1" VerticalAlignment="Center">
            <Button Content="Auto Contour" Click="AutoContourButton_Click"/>
            <Button Content="Dose Limit Checker" Click="DoseLimitCheckerButton_Click"/>
            <Button Content="Dose Limit Editor" Click="DoseLimitEditorButton_Click"/>
            <Button Content="Submit Image and Labels" Click="SubmitImageAndLabelsButton_Click"/>
            <Button Content="Bladder ART" Click="BladderARTButton_Click"/>
        </StackPanel>
    </Grid>
</Window>


