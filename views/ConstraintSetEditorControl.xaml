    <UserControl x:Class="nnunet_client.views.ConstraintSetEditorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:helpers="clr-namespace:nnunet_client"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:local="clr-namespace:nnunet_client.views"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="800"
             x:Name="rootControl">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Margin="0,5,0,5">Prescriptions</TextBlock>

        <local:PrescriptionListDataGridControl 
            x:Name="prescriptionListDataGridControl"
            Width="300"
            Grid.Row="1" 
            />

        <TextBlock Grid.Row="2"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Margin="0,5,0,5">Dose Limits</TextBlock>

        <local:DoseLimitListDataGridControl 
            x:Name="doseLimitListDataGridControl"
            Grid.Row="3"
            />


        
    </Grid>
        
</UserControl>
