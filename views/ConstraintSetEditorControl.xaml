    <UserControl x:Class="nnunet_client.views.ConstraintSetEditorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:helpers="clr-namespace:nnunet_client"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="800"
             x:Name="rootControl">
                 
        
    <Grid Margin="10">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>


        <!-- Title -->
        <StackPanel Orientation="Horizontal" Margin="5" Grid.Row="0">
            <TextBlock Text="Title:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBox Text="{Binding Title, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
             FontSize="14"
             Width="300"/>
        </StackPanel>

        <!-- Prescription Grid -->
        <GroupBox Header="Prescriptions" Margin="0,10,0,10" Grid.Row="1">
            <DataGrid x:Name="PrescriptionGrid"
                      AutoGenerateColumns="False"
                      CanUserAddRows="True"
                      CanUserDeleteRows="True"
                      Margin="5"
                      ItemsSource="{Binding Prescriptions}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="*" />
                    <DataGridTextColumn Header="Total Dose (cGy)" Binding="{Binding TotalDose}" Width="*" />
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

        <!-- PlanContourIds Grid -->
        <GroupBox Header="PlanContourIds" Margin="0,10,0,10" Grid.Row="2">
            <DataGrid x:Name="PlanContourGrid"
              AutoGenerateColumns="False"
              CanUserAddRows="True"
              CanUserDeleteRows="True"
              Margin="5"
              ItemsSource="{Binding PlanContourIds}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Id" Binding="{Binding}" Width="*" />
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>
        

        <!-- Constraint Grid -->
        <GroupBox Header="Constraints" Margin="0" Grid.Row="3">
            <DataGrid x:Name="ConstraintGrid"
                      AutoGenerateColumns="False"
                      CanUserAddRows="True"
                      CanUserDeleteRows="True"
                      ItemsSource="{Binding ContourConstraints}"
                      Margin="5">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="100"/>

                    <DataGridTemplateColumn Header="Plan Contour ID">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding PlanContourId}" VerticalAlignment="Center" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                        <DataGridTemplateColumn.CellEditingTemplate>
                            <DataTemplate>
                                <ComboBox 
                                    ItemsSource="{Binding DataContext.PlanContourIds, ElementName=rootControl}"
                                    SelectedItem="{Binding PlanContourId, Mode=TwoWay}" 
                                />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellEditingTemplate>
                    </DataGridTemplateColumn>
                    
                    
                    
                    <DataGridComboBoxColumn Header="Type"
                                            SelectedItemBinding="{Binding Type}"
                                            ItemsSource="{x:Static helpers:EnumHelper.ContourTypeValues}" />


                    <DataGridTemplateColumn Header="Prescription">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Prescription.Id}" VerticalAlignment="Center" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                        <DataGridTemplateColumn.CellEditingTemplate>
                            <DataTemplate>
                                <ComboBox 
                ItemsSource="{Binding DataContext.Prescription.Ids, ElementName=rootControl}"
                SelectedItem="{Binding Prescrption.Id, Mode=TwoWay}" 
            />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellEditingTemplate>
                    </DataGridTemplateColumn>


                    <DataGridComboBoxColumn Header="Constraint Type"
                                            SelectedItemBinding="{Binding Constraints[0].Type}"
                                            ItemsSource="{x:Static helpers:EnumHelper.ConstraintTypeValues}" />
                    <DataGridTextColumn Header="Constraint"
                                        Binding="{Binding Constraints[0].Limit}"
                                        Width="200"/>
                    <DataGridTextColumn Header="Comment"
                                        Binding="{Binding Constraints[0].Comment}"
                                        Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>
    </Grid>
</UserControl>
