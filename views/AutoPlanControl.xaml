<UserControl x:Class="nnunet_client.views.AutoPlanControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:esapiui="clr-namespace:esapi.UI;assembly=esapi"
             xmlns:local="clr-namespace:nnunet_client.views"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <!-- Label column -->
                <ColumnDefinition Width="*"/>
                <!-- Content column -->
            </Grid.ColumnDefinitions>

            <!-- Course Label -->
            <Label Content="Course:" VerticalAlignment="Center" Margin="5" Grid.Row="0" Grid.Column="0"/>

            <!-- Course Selector -->
            <esapiui:CourseListComboBox x:Name="CourseComboBoxControl"
                                    Margin="5"
                                    Grid.Row="0"
                                    Grid.Column="1" />

            <!-- Image/StructureSet Label -->
            <Label Content="Image/ContourSet:" VerticalAlignment="Center" Margin="5" Grid.Row="1" Grid.Column="0"/>
            <StackPanel Orientation="Horizontal"
                    Grid.Row="1"
                    Grid.Column="1"
                    Margin="5"
                    HorizontalAlignment="Left">
                <Label Content="{Binding Image.Id}"  BorderThickness="1" MinWidth="100" Background="AliceBlue"/>
                <Label Content="{Binding StructureSet.Id}" BorderThickness="1" MinWidth="100" Background="AliceBlue"/>
            </StackPanel>

            
            <!-- Bladder/Rectum/Bowel -->
            <Label Content="Bladder/Rectum/Bowel" VerticalAlignment="Center" Margin="5" Grid.Row="2" Grid.Column="0"/>
            <StackPanel Orientation="Horizontal"
                    Grid.Row="2"
                    Grid.Column="1"
                    Margin="5"
                    HorizontalAlignment="Left">
                <Label Content="{Binding Bladder.Id}" />
                <Label Content="{Binding Rectum.Id}" />
                <Label Content="{Binding Bowel.Id}" />
            </StackPanel>

            <!-- PTV -->
            <Label Content="PTV" VerticalAlignment="Center" Margin="5" Grid.Row="3" Grid.Column="0"/>
            <StackPanel Orientation="Horizontal"
                    Grid.Row="3"
                    Grid.Column="1"
                    Margin="5"
                    HorizontalAlignment="Left">
                
                
                <Label Content="{Binding PTV.Id}" />
                
                
                <Button x:Name="MakePTVButton"
                    Click="MakePTVButton_Click"
                    MinWidth="100"
                    Margin="5"
                    Padding="5">Make PTV</Button>
            </StackPanel>

            <!-- Plan-->
            <Label Content="Plan:" VerticalAlignment="Center" Margin="5" Grid.Row="4" Grid.Column="0"/>
            <StackPanel Orientation="Horizontal"
                    Grid.Row="4"
                    Grid.Column="1"
                    Margin="5"
                    HorizontalAlignment="Left">
                <Label Content="{Binding Plan.Id}" />
                <Button x:Name="OptimizeButton"
                    Click="OptimizeButton_Click"
                    MinWidth="100"
                    Margin="5"
                    Padding="5">Make Plan</Button>
               
            </StackPanel>

            <!-- Constrain Editor -->
            <local:ConstraintSetEditorControl Grid.Row="5"  Grid.ColumnSpan="2" />

        </Grid>
    </ScrollViewer>
</UserControl>
