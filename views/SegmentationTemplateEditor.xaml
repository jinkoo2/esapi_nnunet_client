<UserControl x:Class="nnunet_client.views.SegmentationTemplateEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="800">

    <UserControl.Resources>
        <ResourceDictionary>

            <Style x:Key="DataGridRowCheckBoxStyle" TargetType="CheckBox">
                <Setter Property="VerticalAlignment" Value="Center" />
                <Setter Property="HorizontalAlignment" Value="Center" />
                <Setter Property="Margin" Value="5" />
            </Style>
            
            <Style x:Key="DataGridRowTextBlockStyle" TargetType="TextBlock">
                <Setter Property="VerticalAlignment" Value="Center" />
                <Setter Property="Margin" Value="5" />
            </Style>

            <Style x:Key="DataGridRowTextBoxStyle" TargetType="TextBox">
                <Setter Property="VerticalAlignment" Value="Stretch" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
                <Setter Property="Margin" Value="5" />
            </Style>

        </ResourceDictionary>
    </UserControl.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0 0 0 10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Name:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Right" Background="Transparent"/>
            <TextBox Text="{Binding Template.Name}" Grid.Row="0" Grid.Column="1" Width="300" Margin="5 2" HorizontalAlignment="Left"/>
            <TextBlock Text="Description:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Top" HorizontalAlignment="Right" Background="Transparent"/>
            <TextBox Text="{Binding Template.Description}"
             Grid.Row="1" Grid.Column="1"
                     HorizontalAlignment="Left"
             Width="600" Height="60"
             TextWrapping="Wrap"
             AcceptsReturn="True"
             Margin="5 2"/>
        </Grid>


        <DataGrid ItemsSource="{Binding Template.ContourList}"
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  Margin="0 10 0 0"
                  >
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="100" 
                                    ElementStyle="{StaticResource DataGridRowTextBlockStyle}"
                                    EditingElementStyle="{StaticResource DataGridRowTextBoxStyle}"
                                    />


                <DataGridTemplateColumn Header="Type" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <ComboBox 
                ItemsSource="{Binding DataContext.Template.ContourTypes, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type DataGrid}}}"
                SelectedItem="{Binding Type}"
                />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>


                <DataGridTemplateColumn Header="Color" Width="50">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border Background="{Binding ColorBrush}" Width="20" Height="20" BorderBrush="Black" BorderThickness="1"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                    <DataGridTemplateColumn.CellEditingTemplate>
                        <DataTemplate>
                            <xctk:ColorPicker SelectedColor="{Binding Color, Mode=TwoWay}" Width="140" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellEditingTemplate>
                </DataGridTemplateColumn>


                <DataGridCheckBoxColumn Header="High Resolution" Binding="{Binding HighResolution}" Width="100" 
                                        ElementStyle="{StaticResource DataGridRowCheckBoxStyle}"
                                        EditingElementStyle="{StaticResource DataGridRowTextBoxStyle}"
                                        />

                <DataGridTextColumn Header="Mode ID" Binding="{Binding ModelId}" 
                                    ElementStyle="{StaticResource DataGridRowTextBlockStyle}" 
                                    EditingElementStyle="{StaticResource DataGridRowTextBoxStyle}"
                                    />



                <DataGridTextColumn Header="Model Label" Binding="{Binding ModelLabelName}" Width="100" 
                                    ElementStyle="{StaticResource DataGridRowTextBlockStyle}" 
                                    EditingElementStyle="{StaticResource DataGridRowTextBoxStyle}"
                                    />

                <!-- status -->
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100" 
                                    ElementStyle="{StaticResource DataGridRowTextBlockStyle}"
                                    EditingElementStyle="{StaticResource DataGridRowTextBoxStyle}"
                                    >
                    <DataGridTextColumn.CellStyle>
                        <!-- Style targets the DataGridCell container -->
                        <Style TargetType="DataGridCell">
                            <Setter Property="VerticalContentAlignment" Value="Center" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding StatusCode}" Value="ERROR">
                                    <Setter Property="Foreground" Value="Red"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGridTextColumn.CellStyle>
                </DataGridTextColumn>


            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
