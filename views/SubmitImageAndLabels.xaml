<UserControl x:Class="nnunet_client.views.SubmitImageAndLabels"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:nnunet_client.views"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:collections="clr-namespace:System.Collections;assembly=mscorlib"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800" MinHeight="400">
    <UserControl.Resources>
        <sys:Boolean x:Key="TrueValue">True</sys:Boolean>
        <sys:Boolean x:Key="FalseValue">False</sys:Boolean>
    </UserControl.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Dataset Selection -->
        <GroupBox Header="Dataset Selection" Grid.Row="0" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Label Content="Dataset:" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <ComboBox 
                    Grid.Column="1"
                    ItemsSource="{Binding Datasets}"
                    DisplayMemberPath="name"
                    SelectedItem="{Binding SelectedDataset}"
                    Margin="0,5">
                    <ComboBox.Style>
                        <Style TargetType="ComboBox">
                            <Setter Property="IsEnabled" Value="True"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsLoading}" Value="True">
                                    <Setter Property="IsEnabled" Value="False"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ComboBox.Style>
                </ComboBox>
                <Button 
                    Grid.Column="2"
                    Content="Refresh"
                    Command="{Binding LoadDatasetsCommand}"
                    Margin="10,5,0,5"
                    Padding="10,5">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="IsEnabled" Value="True"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsLoading}" Value="True">
                                    <Setter Property="IsEnabled" Value="False"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>
        </GroupBox>

        <!-- Dataset Info -->
        <GroupBox Header="Dataset Information" Grid.Row="1" Margin="0,0,0,10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Margin="5">
                    <Run Text="Name: "/>
                    <Run Text="{Binding SelectedDataset.name}" FontWeight="Bold"/>
                </TextBlock>
                <TextBlock Grid.Row="1" Margin="5" TextWrapping="Wrap">
                    <Run Text="Description: "/>
                    <Run Text="{Binding SelectedDataset.description}"/>
                </TextBlock>
                <TextBlock Grid.Row="2" Margin="5">
                    <Run Text="ID: "/>
                    <Run Text="{Binding SelectedDataset.id}" FontStyle="Italic"/>
                </TextBlock>
                <Grid Grid.Row="3" Margin="5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Label Grid.Column="0" Content="Image For:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <ComboBox Grid.Column="1"
                        ItemsSource="{Binding ImageForOptions}"
                        SelectedItem="{Binding SelectedImageFor}"
                        HorizontalAlignment="Left"
                        MinWidth="150"
                        Margin="0,5"/>
                </Grid>
            </Grid>
        </GroupBox>

        <!-- Label to Structure Mapping -->
        <GroupBox Header="Label to Structure Mapping" Grid.Row="2" Margin="0,0,0,10">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding LabelMappings}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="150"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock 
                                    Grid.Column="0"
                                    Text="{Binding LabelName}" 
                                    VerticalAlignment="Center"
                                    FontWeight="Bold"/>
                                <TextBlock 
                                    Grid.Column="1"
                                    Text="(Value: " 
                                    VerticalAlignment="Center"
                                    Margin="10,0,0,0"/>
                                <TextBlock 
                                    Grid.Column="1"
                                    Text="{Binding LabelValue}" 
                                    VerticalAlignment="Center"
                                    Margin="70,0,0,0"/>
                                <TextBlock 
                                    Grid.Column="1"
                                    Text=") → " 
                                    VerticalAlignment="Center"
                                    Margin="90,0,0,0"/>
                                <ComboBox 
                                    Grid.Column="2"
                                    ItemsSource="{Binding DataContext.AvailableStructures, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    DisplayMemberPath="Id"
                                    SelectedItem="{Binding SelectedStructure}"
                                    Margin="10,0,10,0"
                                    HorizontalAlignment="Stretch"/>
                                <Button 
                                    Grid.Column="3"
                                    Content="Clear"
                                    Click="ClearButton_Click"
                                    Margin="0,0,0,0"
                                    Padding="5,2"
                                    MinWidth="50"
                                    VerticalAlignment="Center"/>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </GroupBox>

        <!-- Buttons -->
        <StackPanel Orientation="Horizontal" Grid.Row="3" HorizontalAlignment="Center" Margin="0,10">
            <Button 
                Content="Submit"
                Command="{Binding SubmitCommand}"
                MinWidth="100" 
                Margin="10,5" 
                Padding="10,5">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="IsEnabled" Value="True"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsLoading}" Value="True">
                                <Setter Property="IsEnabled" Value="False"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>

        <!-- Status Messages -->
        <StackPanel Grid.Row="4" Margin="0,10,0,0">
            <TextBlock 
                Text="{Binding ErrorMessage}" 
                Foreground="Red" 
                TextWrapping="Wrap"
                Visibility="{Binding ErrorMessage, Converter={x:Static local:NullToVisibilityConverter.Instance}}"/>
            <TextBlock 
                Text="Loading datasets..." 
                Foreground="Blue">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsLoading}" Value="True">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </StackPanel>
    </Grid>
</UserControl>

