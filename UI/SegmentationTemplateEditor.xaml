<UserControl x:Class="nnunet_client.UI.SegmentationTemplateEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Vertical" Margin="0 0 0 10">
            <TextBlock Text="Template Name" FontWeight="Bold" />
            <TextBox x:Name="TemplateNameBox" Width="300" Margin="0 5" />

            <TextBlock Text="Description" FontWeight="Bold" />
            <TextBox x:Name="DescriptionBox" Width="600" Height="60" TextWrapping="Wrap" AcceptsReturn="True"/>
        </StackPanel>

        <DataGrid x:Name="ContourListGrid"
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  CanUserAddRows="True"
                  CanUserDeleteRows="True"
                  Margin="0 10 0 0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="100"/>
                <DataGridComboBoxColumn Header="Type" Width="100"
                                        SelectedItemBinding="{Binding Type}" 
                                        ItemsSource="{Binding DicomTypes}" />
                <DataGridTemplateColumn Header="Color" Width="160">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border Background="{Binding ColorBrush}" Width="20" Height="20" BorderBrush="Black" BorderThickness="1"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                    <DataGridTemplateColumn.CellEditingTemplate>
                        <DataTemplate>
                            <xctk:ColorPicker SelectedColor="{Binding Color, Mode=TwoWay}" Width="140" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellEditingTemplate>
                </DataGridTemplateColumn>
                <DataGridCheckBoxColumn Header="High Resolution" Binding="{Binding HighResolution}" Width="100"/>
                <DataGridComboBoxColumn Header="Model Id" Width="200"
                                        SelectedItemBinding="{Binding ModelId}"
                                        ItemsSource="{Binding ModelIds}"/>
                <DataGridComboBoxColumn Header="Model Label" Width="100"
                                        SelectedItemBinding="{Binding ModelLabel}"
                                        ItemsSource="{Binding ModelLabels}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
