<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- 
        Default Control Base Style - Applies to all controls
    -->
    <Style TargetType="{x:Type Control}">
        <Setter Property="FontSize" Value="11"/>
        <Setter Property="FontFamily" Value="Segoe UI, sans-serif"/>
        <!-- Do not set Margin/Padding here, it causes layout issues for *every* control -->
        <!-- <Setter Property="Margin" Value="0"/>
        <Setter Property="Padding" Value="0"/> -->
    </Style>

    <!-- Colors -->
    <SolidColorBrush x:Key="PrimaryColor" Color="#007ACC"/>
    <SolidColorBrush x:Key="SecondaryColor" Color="#2D2D30"/>
    <SolidColorBrush x:Key="BackgroundColor" Color="#F0F0F0"/>
    <SolidColorBrush x:Key="TextColor" Color="#333333"/>
    
    <!-- Typography -->
    <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="24"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Foreground" Value="{StaticResource PrimaryColor}"/>
        <Setter Property="Margin" Value="0,0,0,10"/>
    </Style>

    <Style x:Key="SubHeaderTextStyle" TargetType="TextBlock" BasedOn="{StaticResource HeaderTextStyle}">
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
    </Style>
    
    <!-- Default Label -->
    <Style x:Key="InputLabelStyle" TargetType="Label">
        <Setter Property="Margin" Value="0,5,0,2"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
    </Style>
    
    <!-- Default TextBox -->
    <Style TargetType="TextBox">
        <Setter Property="Padding" Value="5"/>
        <Setter Property="Margin" Value="0,0,0,10"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="BorderBrush" Value="#CCCCCC"/>
    </Style>
    
    <!-- Default ComboBox -->
    <Style TargetType="ComboBox">
        <Setter Property="Padding" Value="5"/>
        <Setter Property="Margin" Value="0,0,0,10"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="BorderBrush" Value="#CCCCCC"/>
        <Setter Property="Height" Value="30"/>
    </Style>

    <!-- 
        *** Default Button Style (Implicit) ***
        FIXED: Merged the two conflicting button styles into this one.
        FIXED: Changed Border.Background to TemplateBinding so it respects the style's Background setter.
    -->
    <Style TargetType="{x:Type Button}">
        <!-- Base Look & Feel -->
        <Setter Property="Background" Value="{StaticResource PrimaryColor}"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Padding" Value="10,5"/>
        <Setter Property="Margin" Value="4"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Cursor" Value="Hand"/>
        
        <!-- Visual Template -->
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <!-- Use TemplateBinding to ensure the Border uses the Background property defined above -->
                    <Border Background="{TemplateBinding Background}" CornerRadius="3" Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        
        <!-- Interaction States -->
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#005A99"/>
            </Trigger>
        </Style.Triggers>
    </Style>
    
    <!-- Default Data Grid -->
    <Style TargetType="DataGrid">
        <Setter Property="AlternatingRowBackground" Value="#F8F8F8"/>
        <Setter Property="BorderBrush" Value="#CCCCCC"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>
    
    <!-- Default Styles for Status Bar -->
    <Style TargetType="StatusBar">
        <Setter Property="Background" Value="{StaticResource BackgroundColor}"/>
        <Setter Property="BorderThickness" Value="0,1,0,0"/>
        <Setter Property="BorderBrush" Value="#CCCCCC"/>
        <Setter Property="Padding" Value="5,2"/>
    </Style>

    <!-- Default GroupBox -->
    <Style TargetType="{x:Type GroupBox}">
        <Setter Property="Padding" Value="10"/>
        <Setter Property="Margin" Value="0 8 0 8"/>
        <Setter Property="BorderBrush" Value="#D0D0D0"/>
        <Setter Property="Background" Value="White"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type GroupBox}">
                    <Border BorderBrush="{TemplateBinding BorderBrush}" 
                            BorderThickness="{TemplateBinding BorderThickness}" 
                            Background="{TemplateBinding Background}" 
                            CornerRadius="4">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <ContentPresenter Content="{TemplateBinding Header}" 
                                              Margin="10 4 10 0" 
                                              Grid.Row="0"/>
                            <ContentPresenter Grid.Row="1" Margin="{TemplateBinding Padding}"/>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>