<Window x:Class="nnunet_client.SubmitImageAndLabelsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:nnunet_client.views"
        xmlns:local="clr-namespace:nnunet_client.views"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        xmlns:esapiui="clr-namespace:esapi.UI;assembly=esapi"
        Title="Submit Image and Labels" Height="800" Width="600"
        FontFamily="Arial" FontSize="12"
        Closed="Window_Closed" >

    <Window.Resources>
        <ResourceDictionary>
            <Style TargetType="{x:Type TextBlock}">
                <Setter Property="Background" Value="LightGray" />
                <Setter Property="Padding" Value="5" />
                <Setter Property="Margin" Value="5" />
                <Setter Property="MinWidth" Value="100" />
                <Setter Property="MinHeight" Value="25" />
                <Setter Property="VerticalAlignment" Value="Center" />
            </Style>

            <Style TargetType="{x:Type TextBox}">
                <Setter Property="Background" Value="LightGray" />
                <Setter Property="Padding" Value="5" />
                <Setter Property="Margin" Value="5" />
                <Setter Property="MinWidth" Value="100" />
                <Setter Property="MinHeight" Value="25" />
                <Setter Property="HorizontalContentAlignment" Value="Left" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
            </Style>

            <Style TargetType="{x:Type Label}">
                <Setter Property="Background" Value="LightGray" />
                <Setter Property="Padding" Value="0" />
                <Setter Property="Margin" Value="5" />
                <Setter Property="MinWidth" Value="100" />
                <Setter Property="Height" Value="25" />
            </Style>

            <Style TargetType="{x:Type ComboBox}">
                <Setter Property="Background" Value="LightGray" />
                <Setter Property="Padding" Value="5" />
                <Setter Property="Margin" Value="5" />
                <Setter Property="MinHeight" Value="25" />
                <Setter Property="MinWidth" Value="75" />
                <Setter Property="HorizontalAlignment" Value="Left" />
            </Style>

            <Style TargetType="{x:Type Button}">
                <Setter Property="Padding" Value="10, 5" />
                <Setter Property="Margin" Value="5" />
                <Setter Property="Background" Value="#007ACC" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Foreground" Value="White" />
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="#005a99" />
                    </Trigger>
                    <Trigger Property="IsPressed" Value="True">
                        <Setter Property="Background" Value="#004d80" />
                    </Trigger>
                </Style.Triggers>
            </Style>
        </ResourceDictionary>
    </Window.Resources>
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Title Header -->
        <Border Grid.Row="0"
         Background="#EEEEEE"
         CornerRadius="5"
         Margin="5,5,5,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox 
                  Grid.Column="0" 
                  FontWeight="SemiBold"
                  FontSize="18"
                  HorizontalAlignment="Stretch"
                  HorizontalContentAlignment="Center"
                  Background="Transparent"
                  BorderThickness="0"
                  Margin="0,5,0,5"
                  Padding="5"
                  Text="Submit Image and Labels"/>

                <Button x:Name="SavePatient"
                    Grid.Column="1"
                       VerticalAlignment="Center"
                       ToolTip="Save Patient"
                        Click="SavePatient_Click">
                    Save Patient
                </Button>
            </Grid>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Patient Selection -->
            <GroupBox Grid.Row="0" Header="Select Patient" Padding="5" Margin="0,0,0,10">
                <esapiui:AutoCompleteTextBox x:Name="PatientSearchBox"
                     Margin="0,0,0,10"
                     HorizontalAlignment="Stretch"/>
            </GroupBox>

            <!-- Structure Set Selection -->
            <GroupBox Grid.Row="1" Header="Select Structure Set" Padding="5" Margin="0,0,0,10">
                <ComboBox x:Name="StructureSetComboBox"
                    ItemsSource="{Binding StructureSets}"
                    DisplayMemberPath="Id"
                    SelectedItem="{Binding SelectedStructureSet}"
                    HorizontalAlignment="Stretch"
                    Margin="0,5"/>
            </GroupBox>

            <!-- Submit Image and Labels Control -->
            <GroupBox Grid.Row="2" Header="Dataset and Label Mapping" Padding="5">
                <views:SubmitImageAndLabels 
                    DataContext="{Binding SubmitImageAndLabelsViewModel}"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"/>
            </GroupBox>
        </Grid>
    </Grid>
</Window>

